How to create and compile a basic TS project ? 

    ### to create ats project we need to create a node project or sometime you will use it in react as well
        npm init -y
    ### to install all the types 
        npm install --save-dev @types/node
    ### to make a tsconfig tp define the typescript compiler options 
        

      
